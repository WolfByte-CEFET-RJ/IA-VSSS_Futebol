# Screen dimensions
WIDTH, HEIGHT = 800, 600
SCALE = 1.5
# Agent properties
ROBOT_SIZE = int(45 * SCALE)
TARGET_RADIUS = int(10 * SCALE)

# Time properties
FPS = 60
TIME_STEP = 0.1

# Colors
WHITE = (255, 255, 255)
RED = (255, 0, 0)
BLUE = (0, 0, 255)
ORANGE = (255, 127, 0)

# Generation duration
GENERATION_DURATION = 6  # seconds per generation
